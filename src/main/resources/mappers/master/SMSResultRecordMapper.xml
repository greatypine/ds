<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.guoanshequ.dc.das.dao.master.SMSResultRecordMapper">

    <select id="addSMSResultRecord" parameterType="SMSResultRecordEntity"  >
        <![CDATA[
		  insert  into  t_sms_result_record(app_key,channelid,mobilephone,exeresult,createtime) values (#{appKey},#{channelId},#{mobilePhone},#{exeResult},#{createTime});
		]]>
    </select>

</mapper>