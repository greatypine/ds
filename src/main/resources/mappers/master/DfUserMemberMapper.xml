<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.guoanshequ.dc.das.dao.master.DfUserMemberMapper">

	<insert id="addDfUserMember" parameterType="java.util.Map" >
		<![CDATA[
			insert ignore into df_user_member (
				customer_id,mobilephone,idcard,birthplace,born_province,born_city,birthday,sex,
				regist_cityno,regist_time,opencard_time,isnew_member,associator_level,associator_expiry_date
			) values (
				#{customer_id},#{mobilephone},#{idcard},#{birthplace},#{born_province},#{born_city},#{birthday},#{sex},
				#{regist_cityno},#{regist_time},#{opencard_time},#{isnew_member},#{associator_level},#{associator_expiry_date}
			)
        ]]>
	</insert>

</mapper>